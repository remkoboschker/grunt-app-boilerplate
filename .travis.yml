language: node_js
node_js:
  - 0.8
before_script:
  - npm install grunt-cli
  - export PATH=$PATH:`pwd`/node_modules/.bin
  - gem install sass
script:
  - phantomjs -v
  - grunt
  - grunt test:mocha --debug